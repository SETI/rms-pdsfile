# PR-24 sub-plan — ruff-clean the rule modules, the tools and the test tree

**Date:** 2026-08-04
**Parent plan:** `plans/2026-07-25-modernization-plan.md` §5 Phase 5, PR-24 (size L)
**Governing addendum:** `plans/2026-08-03-addendum-pr23-24-owner-decisions.md`
(ACKNOWLEDGED; decision 3 removes the formatting half of this PR, decision 4 makes
it unstacked)
**Branch:** `pr-24-ruff-rest`, based on `rewrite` @ `8cab66a`
**GitHub base:** `rewrite`. Reviewer diff is `git diff origin/rewrite...HEAD`.
**Scope:** `ruff check` only, over everything PR-23 did not take. No behavior
change, no test added, no golden touched — so the §6.2 gate is an **identical**
per-test id set in both modes, and movement in either direction is a hard stop.

PR-24 is marked **(L)**, so §6.4 step 1 requires this file before any code change.
`plans/2026-08-03-pr-23-subplan.md` is the model: the sections below are written
first and an "as executed" delta is appended at the end rather than edited in.

## 0. What this PR is *not*

The plan's PR-24 heading still reads `style: ruff-clean **and format** rules and
remaining files`. PR-23 corrected its own heading and left this one. It is stale
for the same reason: the churn checkpoint ran on 2026-08-03 and the owner dropped
the reformat entirely (addendum decision 3). In this PR:

- `ruff format` is **not run**. Running it is itself a §6.4 hard stop.
- `ruff format --check` is **not enabled**; `ENABLE_RUFF_FORMAT` stays `false`.
- No `# fmt: off` / `# fmt: on` guard is added. They exist only to protect
  aligned blocks from a formatter that will not run.
- The stale heading is corrected here, in its own plan-only commit.

## 1. File list, and why each file is in or out

PR-23 took the fifteen modules directly under `src/pdsfile/`. This PR takes the
rest of the tracked Python.

| Path | In? | Why |
|---|---|---|
| `src/pdsfile/pds3file/__init__.py` | **in** | named in the plan's PR-24 text |
| `src/pdsfile/pds3file/rules/*.py` (26 files) | **in** | the rule modules, the PR's headline scope |
| `src/pdsfile/pds4file/__init__.py` | **in** | named in the plan's PR-24 text |
| `src/pdsfile/pds4file/rules/*.py` (8 files) | **in** | the pds4 rule modules |
| `src/pdsfile/holdings_maintenance/pds3/*.py` (10 files) | **in** | named in the plan, including `re_validate.py` and `shelf_consistency_check.py` |
| `src/pdsfile/holdings_maintenance/pds4/*.py` (6 files) | **in** | same |
| `src/pdsfile/tools/show_opus_products.py` | **in** | **deferred entry 70**: it carries an `I001` ratchet entry and belongs to no PR. The plan does not name it and PR-23 excluded it as "a subpackage module". This is the last ruff PR, so it is taken here and the entry is closed |
| `tests/**` except the two frozen files below | **in** | the `tests/rules/**` files PR-08 migrated, plus `tests/conftest.py`, `tests/pds3file/`, `tests/pds4file/`, `tests/support/`, `tests/core/`, `tests/holdings_maintenance/` |
| `scripts/check_runtime_imports.py` | **in** | `scripts/` is in scope and this file is not one of the four prohibited ones |
| the fifteen modules directly under `src/pdsfile/` | **out** | PR-23's; already clean |
| `src/pdsfile/_version.py` | **out** | generated by setuptools-scm and `.gitignore`d (`**/_version.py`), so it is absent from a fresh checkout. Not edited, and **no ratchet entry is added for it** — deferred entry 71 explains why no gate sees it |
| `scripts/dump_public_api.py` | **out** | §6.4 forbids editing it. It carries a `RUF100` ratchet entry, which is therefore **permanent by prohibition, not by freeze** — see §5 |
| `tests/api/test_api_freeze.py`, `tests/api/api_manifest.json`, `tests/api/manifest_allowlist.json` | **out** | §6.4 forbids editing them outright |

`tests/api/conftest.py`, `tests/api/test_mixin_collisions.py` and
`tests/api/test_mixin_import_isolation.py` are **in** scope but have zero
violations, so they produce no diff.

## 2. How the violation set is derived

The plan says not to trust its own list, and the list is dated 2026-07-17.
Method, identical to PR-23's: run `ruff check` with the **template select set and
no per-file-ignores at all**, so nothing the committed ratchet suppresses can hide.

```
target-version = "py310"     # NOT py39 — the UP set differs
line-length = 100
[lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4", "A", "N", "PT", "RUF"]
extend-ignore = ["PT011", "SIM105", "SIM108"]
```

`ruff 0.15.22` over the in-scope paths reports **2,760 violations**:

| Path | Violations |
|---|---:|
| `src/pdsfile/pds3file/rules/` | 1,241 |
| `src/pdsfile/pds4file/rules/` | 629 |
| `tests/` | 538 |
| `src/pdsfile/holdings_maintenance/` | 298 |
| `src/pdsfile/pds3file/__init__.py` | 40 |
| `src/pdsfile/pds4file/__init__.py` | 11 |
| `scripts/` | 2 |
| `src/pdsfile/tools/` | 1 |

By code: `E501` 1641 · `W191` 302 · `PT006` 240 · `UP031` 144 · `I001` 76 ·
`F401` 37 · `RUF012` 33 · `N801` 31 · `RUF059` 28 · `N806` 28 · `N999` 25 ·
`E701` 23 · `B007` 20 · `A001` 14 · `C405` 13 · `F841` 10 · `B006` 9 · `F811` 8 ·
`PT017` 7 · `PT028` 6 · `UP015` 6 · `UP024` 6 · `A002` 6 · `E721` 5 · `N802` 5 ·
`RUF005` 4 · `SIM115` 3 · `B012` 3 · `UP004` 3 · `E711` 3 · `RUF100` 2 ·
`RUF015` 2 · `RUF022` 2 · `PT015` 2 · `B011` 2 · and fifteen codes with one site
each (`F541`, `RUF051`, `UP034`, `W291`, `E401`, `F821`, `SIM102`, `N805`,
`PT012`, `PT014`, `E712`).

## 3. Classification rule

Every violation is either **fixed** or **permanently excluded**. The rule, stated
before the table so it can be checked against it, is PR-23's:

**Fix** a violation only when the rewrite is provably behavior-identical **from
evidence local to the same function, or from an exhaustive enumeration of the
assignments to the name involved**.

**Exclude** it permanently when any of:
- **(a)** the fix changes a frozen public name, signature or manifest member;
- **(b)** the only fix is a `ClassVar` annotation — ground rule 5;
- **(c)** the fix requires rewriting a hand-aligned block — the same objection
  that got `ruff format` dropped on 2026-08-03;
- **(d)** equivalence cannot be established locally and rests on a whole-program
  argument, or on the behavior of a third-party callee;
- **(e)** the fix changes the value, the type, the emitted text or the exception
  raised on any path — including log text and frozen sidecar output, which §2
  names as observable;
- **(f)** the fix is a code change beyond a style PR's warrant whose scope the
  owner has not settled;
- **(g)** the file may not be edited at all (§6.4 prohibition).

## 4. The two rules this PR is asked to state

### 4.1 The logging-call rule (§6a of the brief, deferred entry 79)

`UP031`'s ruff fix is an f-string, and the owner's logging rule forbids f-strings
in logging calls: a logging call passes a `%`-style format string and the values
as *arguments*. The lazy conversion is **not** mechanical here, because
`pdslogger.PdsLogger.log` (venv `pdslogger/__init__.py:1484`, and `.open()` at
`:1236`) resolves a lone positional argument as the keyword-only `filepath` when
the message contains no `%` pattern. So

```python
logger.info('Label found for %s' % abspath, label_abspath)   # today
logger.info('Label found for %s', abspath, label_abspath)    # TypeError
logger.info('Label found for %s', abspath, filepath=label_abspath)   # correct
```

An AST sweep of `src/pdsfile/` measured **132** logging calls that build their
message eagerly, **69** of which also pass a positional filepath argument and so
would break under the naive rewrite. (Deferred entry 79 and the coordinator's
scoping both say 130/67; both used a `LOG_METHODS` set missing `pdslogger`'s
`normal()`, which adds `pds4checksums.py:119` and `:128`. The corrected sweep is
recorded in `critiques/pr-24/`.)

**The rule, applied to all 144 `UP031` sites:**

> `UP031` is converted to an f-string only where the site is an **exception
> message** or a **`print()`** — the two places where an f-string is
> unambiguously this project's style and nothing observable depends on the
> `%` spelling. It is **permanently excluded** on (i) logging calls, (ii)
> `file.write()` calls that emit the frozen sidecar formats, and (iii) any `%`
> that sits in a hand-aligned column block.

Which gives **5 fixed** (3 exception messages in `COUVIS_0xxx.py`, 2 `print()`s
in `shelf_consistency_check.py`) and **139 excluded** (46 logging, 24
`file.write`, 69 aligned or otherwise locked). Converting the logging calls is a
code change beyond a `ruff check` PR's warrant — entry 79 says so in terms — and
the owner has not scoped it; rule (f).

`pdsviewable.py:529` shows the filepath bug already live in the tree. It is
PR-23's file and entry 79 owns it; it is **not** touched here.

### 4.2 The prose rule (§6f of the brief, deferred entry 77)

> **A comment or docstring is edited only where a fix in this PR made its
> existing text factually wrong.** Nothing else — no rewrapping, no
> modernization, no tidying of prose the diff did not invalidate.

This closes deferred entry 77. It is also what settles **deferred entry 44**
(`20..` in a log-filename regex): no fix in this PR touches that regex, so under
this rule it is not edited here. The entry stays open and is re-pointed at the
first PR that has a reason to touch the line.

## 5. The permanent set — 2,255 violations

Grouped by reason. Every code that survives in `per-file-ignores` appears here.

### Frozen public names and signatures — rule (a)

| Code | n | Where | Reason |
|---|---:|---|---|
| `N801` | 31 | rule modules | the rule class names (`COISS_xxxx`, `cassini_iss`, …) are the frozen public classes |
| `N999` | 25 | pds3 rule modules | invalid module names — the module names *are* the frozen volset identifiers |
| `N802` | 5 | `COISS_xxxx`, `COUVIS_0xxx`, `COVIMS_0xxx` ×2, `RPX_xxxx` | frozen uppercase methods (`DATA_SET_ID`, `FILENAME_KEYLEN`) |
| `N805` | 1 | `COVIMS_0xxx.py:325` | `OPUS_ID_TO_PRIMARY_LOGICAL_PATH(opus_id)` takes no `self`; `@staticmethod` would change the manifest kind |
| `A002` | 3 | `pds3file/__init__.py:148,151,204` | the frozen `dir=` parameter of `log_path_for_volset`/`log_path_for_volume`, passed by keyword from the tools |
| `F401` | 31 | `pds3file/__init__.py` ×25, `pds4file/__init__.py` ×6 | the `from .rules import (…)` blocks. All 31 names are **manifest members** of `pdsfile.pds3file` / `pdsfile.pds4file`, and the import also carries the side effect that registers the rule subclasses. Deleting one is a manifest break outside both forgiveness categories. PR-23's `import x as x` re-export form was considered and rejected: unlike its two single-name stdlib re-exports, this is a 25-name block whose primary purpose is the registration side effect, and the plan and §6d of the brief both specify a permanent `F401` ignore here |
| `B006` | 9 | `pdschecksums.py` ×1, `pdsdependency.py` ×7, `pdsinfoshelf.py` ×1 | mutable defaults in public tool-function signatures (`oldpairs=[]`, `limits={}`, `handlers=[]`, `messages=[]`, `exceptions=[]`, `old_infodict={}`). The None-sentinel rewrite changes the signature `inspect.signature` reports for functions whose tool contract §2 freezes, and at five sites the default flows into `pdslogger`'s `open(limits=…, handler=…)`, so "the callee never mutates it" is not locally provable — rule (d) as well as (a). One rule for all nine |
| `PT028` | 6 | `crlf.py:29`, `pdsdependency.py:996` | `test_crlf(...)` and `test(...)` are **tool functions**, not pytest tests; `PT` only fires because of the name. Renaming them changes the tools' public API |

### Ground rule 5 — rule (b)

| Code | n | Where | Reason |
|---|---:|---|---|
| `RUF012` | 33 | rule modules (29), `pds3file/__init__.py` ×2, `pds4file/__init__.py` ×2 | mutable class-attribute defaults (`OPUS_TYPE`, `LOCAL_PRELOADED = []`, `SUBCLASSES = {}` …). The only ruff fix is a `ClassVar` annotation, forbidden by ground rule 5 |

### Hand-aligned blocks — rule (c)

| Code | n | Where | Reason |
|---|---:|---|---|
| `E501` | 1641 | 42 files | the `TranslatorByRegex` tables, the `pytest.mark.parametrize` data tables, and long path/regex literals that cannot be split without breaking a semantic unit. In 41 of the 42 files at least one **live table row** locks the code regardless of what is done to the comments, so wrapping the 245 long comments would remove no ratchet code; in the 42nd (`tests/rules/pds4/test_uranus_occs_earthbased.py`) all five are commented-out rows of the same aligned table |
| `W191` | 302 | `cassini_iss_fring_mosaics_rsfrench2025_primary_filespec.py` | the whole file is one tab-indented table. Retabbing *is* the forbidden table rewrite |
| `RUF022` | 2 | `pds3file/rules/__init__.py:10`, `pds4file/rules/__init__.py:12` | `__all__` is hand-aligned with padding spaces before the commas (`"COISS_xxxx" ,`). RUF022's sort rewrites the block and destroys the padding |
| `UP031` | 69 | see §4.1 | `pdsdependency.py` ×38 (the aligned `PdsDependency(...)` argument blocks at `:364–380`, where the `%` operators line up at a fixed column), the ten aligned `--log` help-text blocks, `COCIRS_xxxx.py:516–518` (a `%` inside a raw regex inside a translator table), and the `re_validate.py` continuation blocks |

### Observable output and exception behavior — rule (e)

| Code | n | Where | Reason |
|---|---:|---|---|
| `UP031` | 70 | 46 logging calls + 24 `file.write` calls | §4.1. The `file.write` sites emit the frozen `.py` sidecar formats that PR-13's tool tests compare byte for byte (`'(%11d, %3d, '`, `'"%-33s, '`, `'%4d'`) |
| `B012` | 3 | `pdschecksums.py:369`, `pdsinfoshelf.py:424`, `pdslinkshelf.py:1374` | `return` inside `finally` swallows an in-flight exception. Removing it changes what propagates out of the tools — the exit-code path §2 freezes |
| `SIM115` | 3 | `pdsarchives.py:230`, `pdschecksums.py:44,289` | wrapping in `with` changes *when* the file closes, which in these tools orders against a later read and against `tarfile` finalization |
| `RUF015` | 2 | `pdsinfoshelf.py:580`, `pds4infoshelf.py:565` | `list(d.keys())[0]` raises `IndexError` on an empty dict; `next(iter(d))` raises `StopIteration`. The dict is not proven non-empty. Same reasoning PR-23 applied to `pdscache.py:622` |
| `PT017` | 7 | `test_pds3file_blackbox.py` ×2, `test_pds3file_whitebox.py` ×5 | rewriting `try/except/assert in str(e)` as `pytest.raises(...)` changes *what the test asserts* and how it fails. A test that stops asserting what it asserted is a behavior change in the suite even when the id set is identical |
| `PT015`, `B011` | 2 + 2 | `test_pds3file_blackbox.py:953,968` | the same two `assert False # pragma: no cover` lines, flagged by both rules. They are the unreachable arm of the `PT017` blocks above and move with them |
| `PT012` | 1 | `test_pds3file_whitebox.py:903` | same class: narrowing the `pytest.raises` block changes what is asserted |
| `PT014` | 1 | `test_pds4file_blackbox.py:137` | a **duplicate parametrize case**. Removing it removes a test id — the one thing this PR's gate forbids. Recorded as a new deferred observation |
| `F821` | 1 | `shelf_consistency_check.py:63` | `error += 1` is a typo for `errors`; the line raises `NameError` when reached. Fixing it is a behavior change, and **deferred entry 6 already assigns it to PR-28**, which gives this tool a `main()` |

### Owner-chosen style — permanent, not residue

| Code | n | Where | Reason |
|---|---:|---|---|
| `RUF005` | 4 | `pdsinfoshelf.py:243`, `pds4infoshelf.py:246`, `re_validate.py:820,824` | `x + [y]` is the spelling this project uses; `[*x, y]` is **not wanted anywhere** (owner, 2026-08-03; PR-23 reverted seven). These three files already carry `RUF005`; the entries stay |

### Prohibited to edit — rule (g)

| Code | n | Where | Reason |
|---|---:|---|---|
| `RUF100` | 1 | `scripts/dump_public_api.py:148` | an unused `# noqa: BLE001`. §6.4 forbids editing this file, so the entry is **permanent by prohibition**, not by freeze — it would be a one-character fix in any other file |

## 6. The fixed set — 505 violations

| Code | n | Fix | Why it is behavior-identical |
|---|---:|---|---|
| `PT006` | 240 | `'a,b'` → `('a', 'b')` | **proved, not assumed**: pytest derives ids from argvalues, not argnames. Applied to all 19 files and the collect-only id set re-measured — 892 ns / 558 s ids, byte-identical to `8cab66a`. ruff marks this fix "unsafe"; the id measurement is what discharges that |
| `I001` | 76 | isort ordering | the in-scope import blocks are short flat lists (`import re` / `import translator` / `import pdsfile.pds3file as pds3file`), not the aligned continuation blocks that locked `pdsfile.py:84` for PR-23. Verified per file that no fix reflows an aligned block |
| `RUF059` | 28 | unused unpack target → `_name` | renaming an unpacking target binds a different name and drops nothing; every RHS is still evaluated |
| `N806` | 28 | uppercase local → lowercase | all are locals (`TESTS`, `TESTFILE_PATH`, `LOGGER`) inside test helpers and rule tests; none is a fixture name pytest resolves or part of any signature |
| `E701` | 23 | `if x: y` → two lines | statement-for-statement identical |
| `B007` | 20 | unused loop variable → `_name` | checked per site that the name is not used by a `del`, a closure, or after the loop |
| `A001` | 14 | `dir`/`hex` locals renamed | all are function-local bindings in the maintenance tools; no module-scope or manifested name involved |
| `C405` | 13 | `set([...])` → `{...}` | the set literal builds the same set from the same elements; the intermediate list was the only difference |
| `F841` | 10 | drop the unused binding | checked per site that the RHS has no side effect. `pdsarchives.py:554` is `proceed = False`; the nine test sites are constants or calls whose result is discarded and whose call is kept as an expression statement where it had an effect |
| `F811` | 8 | delete the **dead** definition | `pds3file/__init__.py` ×7: the six alias properties and `log_path_for_volume` are defined twice; the **second** definition is the live one, so the **first** block's copies are deleted. Bodies are semantically identical (one positional, one keyword form) and the signatures are character-identical, so the manifest sees no change. `COVIMS_0xxx.py`: the dead translator-table assignment shadowed by the live method is deleted, keeping the method |
| `F401` | 6 | drop the unused test import | test-module imports only; none is a manifest member |
| `UP024` | 6 | `IOError` → `OSError` | `IOError is OSError` in Python 3, so even the class name in a traceback is `OSError` today |
| `UP015` | 6 | `open(p, 'r')` → `open(p)` | `'r'` is the default mode |
| `E721` | 5 | `type(a) == B` → `type(a) is B` | **never `isinstance`** — this package is a subclass hierarchy and `isinstance` accepts subclasses. `type(x) is C` differs from `type(x) == C` only if the metaclass overrides `__eq__`; every class here has metaclass `type` |
| `UP031` | 5 | → f-string | §4.1: 3 exception messages, 2 `print()`s, all `%s`/`%d` on values provably `str`/`int` |
| `E711` | 3 | `== None` → `is None` | the compared values are `str`/`None` and `Pds3File`/`None`; neither type defines `__eq__`, so `==` already fell back to identity |
| `UP004` | 3 | drop `(object)` | MRO dumped before and after and compared |
| `A002` | 3 | test parameter `dir` renamed | `test_pds3file_blackbox.py` ×3; a parametrize argname and the matching test parameter, renamed together. Ids come from argvalues, so the id set is unchanged (re-measured) |
| `F541` | 1 | drop the `f` prefix | `f'...'` with no placeholder is the same object as `'...'` |
| `RUF051` | 1 | `if k in d: del d[k]` → `d.pop(k, None)` | same effect, same absence of a raise |
| `UP034` | 1 | drop extraneous parentheses | grouping unchanged |
| `W291` | 1 | strip trailing space in a comment | whitespace inside a comment |
| `E401` | 1 | `import os, sys` → two lines | same two bindings |
| `SIM102` | 1 | collapse nested `if` | the outer body is exactly the inner `if`, so `A and B` preserves both the order and the short-circuit |
| `E712` | 1 | `res != False` → `res is not False` | **not** ruff's suggested `if res:` — `viewset` returns either `False` or a `PdsViewSet`, and a truthiness test would also take the else-branch for a falsy viewset |
| `RUF100` | 1 | drop the dead `# noqa: BLE001` | `BLE001` is not in the select set. The explanatory text is kept as a plain comment |

## 7. Ratchet arithmetic

`per-file-ignores` may only **shrink**, measured against `origin/rewrite` @
`8cab66a` — not against any intermediate state of this branch. The before/after
table, per file, goes in the PR description and in
`.cursor/rules/pdsfile_overrides.mdc` deviation (4). Properties to prove:

- no file's "after" set contains a code its `8cab66a` set did not;
- no entry gains a file; a new entry is legitimate only as a **split**;
- every "after" code appears in §5 with a reason;
- **no inline `noqa` is added anywhere** — the ratchet's own comment forbids it,
  and it is checked mechanically (`git diff | grep noqa`).

`scripts/gen_ruff_ratchet.py` is **not** used: deferred entry 33 records that it
emits an empty block against a tree whose committed ignores already suppress
everything, which is exactly this tree. The block is hand-derived from the
no-ignores run in §2 and then verified by running the project config and
requiring `ruff check src/pdsfile tests scripts` to be clean.

## 8. Deferred entries this PR owns

| # | Disposition |
|---|---|
| **31** | `pdsfile/__init__.py`'s `from pdsfile import *`. Both readings change the frozen surface, one shrinking and one growing. **Needs an owner decision that has not been given — not acted on.** The file is PR-23's, not this PR's. Entry stays open |
| **34** | Largely withdrawn (entry 57). What stands is portability: `src/`, `tests/` and `.github/` must resolve holdings roots through `PDS3_HOLDINGS_DIR`/`PDS4_HOLDINGS_DIR`. `tests/pds3file/test_pds3file_whitebox.py` is checked on that ground only; `plans/` and `critiques/` are not scrubbed |
| **44** | `20..` → `\d{4}` in a log-filename regex. Behavior-neutral, but no ruff rule requires it, so §4.2's prose rule leaves it. Entry stays open, re-pointed |
| **52** | `OPUS_PRODUCTS` module table vs the mixin method of the same name. A rename is a public-surface change; a comment is not. Any comment added describes the code as it is, with no reference to plans, critiques, entries, PR or phase numbers, or the frozen surface |
| **70** | `show_opus_products.py` — **taken here**, `I001` fixed, entry closed |
| **76** | Preview rule `E1` is not enabled, so `python.mdc`'s indentation rule is not in force. Owner-level decision about the whole tree; preview rules are **not** enabled. Entry stays open |
| **77** | Settled by §4.2. Entry closed |
| **79** | The eager-logging inventory. Its 130/67 figures are **corrected to 132/69** here (the sweep's method set omitted `pdslogger.normal`). No call is converted; entry stays open |
| **6** | Owns the `shelf_consistency_check.py` `error`/`errors` typo and assigns it to PR-28. Confirmed still open; the `F821` exclusion in §5 points at it |
| **33** | Confirmed still accurate (see §7) |

## 9. Commit sequence

Renames and fixes are never mixed (§2 PR discipline).

1. `docs: correct PR-24's stale heading in the modernization plan` — plan only.
2. `docs: add the PR-24 sub-plan` — this file.
3. `style: ruff-clean the pds3file and pds4file rule modules`.
4. `style: ruff-clean the pds3file and pds4file package initializers` — including
   the `F811` de-duplication.
5. `style: ruff-clean the holdings-maintenance tools`.
6. `style: ruff-clean the tools and scripts entry points`.
7. `style: ruff-clean the test tree`.
8. `build: shrink the ruff ratchet to the freeze-locked set` — `pyproject.toml`
   plus `.cursor/rules/pdsfile_overrides.mdc` deviation (4), which records the
   same set.
9. `docs: record PR-24 validation` — `critiques/phase5-validation.md`,
   `critiques/pr-24/`, `critiques/deferred-observations.md`.

## 10. Verification

| Gate | How |
|---|---|
| §6.2 full-data `--mode ns` | serial, under coverage, as `scripts/automated_tests/pdsfile_main_test.sh` runs it, `-rA --junitxml`. Baseline re-measured in a worktree at `8cab66a`: **858 passed / 34 skipped, 892 ids**. Gate: **identical id-by-id set** |
| §6.2 full-data `--mode s` | same; baseline **555 passed / 3 skipped, 558 ids**. Gate: identical set |
| per-test id set | because this PR edits the files that *generate* test ids, the id set — not the counts — is the gate, and a fast `--collect-only` id diff is run after every commit that touches `tests/` |
| non-vacuity | `coverage.CoverageData.measured_files()` must list **72** `src/pdsfile/*.py` files including all 36 rule modules |
| no-holdings | `scripts/run-all-checks.sh` with no holdings env vars; baseline **92 passed / 800 skipped**. Needs a `venv/` symlink inside the worktree or it fails with "Virtual environment not found", which is not a real gate failure |
| API freeze | `tests/api/test_api_freeze.py` — **the gate is the test, not a hand diff of the dumper's output against the committed manifest**, which differ on a clean tree by design (the PR-08 allowlist category). Plus fresh dumps at base and HEAD compared byte for byte |
| MRO | `__mro__` dumped before and after for the three `UP004` classes |
| ruff | `ruff check src/pdsfile tests scripts` clean with the project config, **and** the no-ignores run over the in-scope files reports exactly the §5 set and nothing else |
| clean install | `scripts/clean_install_check.sh` |
| consumer smoke | `critiques/baselines/consumer-smoke-baseline.md`; gate is **same outcome** — rms-opus 4/4, rms-viewmaster 5 ok / the same 3 failures. *Fewer* failures is as much a flag as more |
| §6.6 | fresh, no-context opus-class reviewer per round, hard cap 4. The full-data record is **regenerated in every round that changes `src/` or `tests/`**; no stale record is carried forward |

## 11. As executed

Appended after execution.
