# PR-23 sub-plan — ruff-clean the core modules

**Date:** 2026-08-03
**Parent plan:** `plans/2026-07-25-modernization-plan.md` §5 Phase 5, PR-23 (size L)
**Governing addendum:** `plans/2026-08-03-addendum-pr23-24-owner-decisions.md`
(ACKNOWLEDGED — four owner decisions, of which decision 3 removes half of what
this PR used to be)
**Branch:** `pr-23-ruff-core`, based on `rewrite` @ `96e5960`
**GitHub base:** `rewrite`. PR-23 and PR-24 are **not stacked** (addendum
decision 4), so the reviewer diff is `git diff origin/rewrite...HEAD`.
**Scope:** `ruff check` only, over the fifteen modules directly under
`src/pdsfile/`. No behavior change, no test added, no golden touched — so the
§6.2 gate is an **identical** per-test pass/fail set in both modes, and movement
in either direction is a hard stop.

PR-23 is marked **(L)**, so §6.4 step 1 requires this file before any code change.
`plans/2026-07-27-pr-21-subplan.md` is the model: the sections below are written
first and an "as executed" delta is appended at the end rather than editing them.

## 0. What this PR is *not*, any more

The plan's PR-23 heading still reads `style: ruff-clean **and format** core
modules`. That is stale. The churn checkpoint the plan made this PR conditional
on **ran on 2026-08-03** against merged `rewrite` — 14 of the 15 target files, ~2,310
changed lines — and the owner **dropped the reformat entirely** (addendum decision
3). Consequently, in this PR:

- `ruff format` is **not run**. Running it is now itself a §6.4 hard stop.
- `ruff format --check` is **not enabled**; `ENABLE_RUFF_FORMAT` stays `false`.
- No `# fmt: off` / `# fmt: on` guard is added anywhere. They exist only to
  protect aligned blocks from a formatter that will not run.
- The plan's stale PR-23 heading is corrected here, in its own commit.

## 1. File list, and why each file is in or out

The scope is "everything directly under `src/pdsfile/`" — the subpackages
(`pds3file/`, `pds4file/`, `holdings_maintenance/`, `tools/`) are PR-24's.
`ls src/pdsfile/*.py` gives **fifteen** tracked files, plus `_version.py` when
one is present:

| File | In? | Why |
|---|---|---|
| `pdsfile.py` (1,939) | **in** | the core module named in the plan |
| `_associations.py` (373) | **in** | extracted core module (PR-20) |
| `_derived_paths.py` (314) | **in** | extracted core module (PR-18) |
| `_index_rows.py` (328) | **in** | extracted core module (PR-19) |
| `_local_fs.py` (437) | **in** | extracted core module (PR-17) |
| `_opus.py` (304) | **in** | extracted core module (PR-19) |
| `_path_utils.py` (219) | **in** | extracted core module (PR-16) |
| `_preload.py` (578) | **in** | extracted core module (PR-21) |
| `_properties.py` (1,686) | **in** | extracted core module (PR-22) |
| `_shelves.py` (356) | **in** | extracted core module (PR-17) |
| `_sorting.py` (525) | **in** | extracted core module (PR-20) |
| `pdscache.py` (1,044) | **in** | named in the plan's PR-23 core list |
| `pdsviewable.py` (583) | **in** | named in the plan's PR-23 core list |
| `preload_and_cache.py` (16) | **in** | directly under `src/pdsfile/`; measured, and it has **zero** violations, so it produces no diff |
| `__init__.py` (12) | **in** | directly under `src/pdsfile/`; the plan's PR-24 text about "`__init__.py` star imports" is about the `pds{3,4}file/__init__.py` pair, which stays PR-24's |
| `_version.py` | **out** | **generated** by setuptools-scm (`write_to` in `pyproject.toml`) and `.gitignore`d (`**/_version.py`), so it is not even present in a fresh checkout. Not edited, and no ratchet entry is added for it |

`src/pdsfile/tools/show_opus_products.py` carries a ratchet entry (`I001`) and is
**out**: it is a subpackage module, not a file directly under `src/pdsfile/`.
Recorded for PR-24.

## 2. How the violation set is derived

The plan says explicitly not to trust its own list, and the list is demonstrably
stale: it puts `A002` in `pdsfile.py`, but PR-18 moved those three methods into
`_derived_paths.py` (**deferred entry 45**, which names PR-23 as its owner).

Method: run `ruff check` with the **template select set and no per-file-ignores at
all**, so nothing the committed ratchet already suppresses can hide:

```
[lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "C4", "A", "N", "PT", "RUF"]
extend-ignore = ["PT011", "SIM105", "SIM108"]
```
with `target-version = "py310"` and `line-length = 100`, i.e. `pyproject.toml`'s
`[tool.ruff]`/`[tool.ruff.lint]` minus `per-file-ignores`.

`ruff 0.15.22` reports **154 violations across 14 of the 15 files**;
`preload_and_cache.py` is the clean one. The coordinator's scoping measurement
said 155; the
one-violation difference is not reconciled and does not matter — the plan's
instruction is to derive the set, and every disposition below is keyed to a
`file:line` in the derived set, not to a count.

## 3. Classification rule

Every violation is either **fixed** or **freeze-locked**. The plan's three
freeze-lock reasons are "changes a frozen public signature/name", "needs an inline
type annotation (ground rule 5)", and "requires reformatting an aligned table",
and it says to expect others. The rule used here, stated before the table so that
it can be checked against the table:

**Fix** a violation only when the rewrite is provably behavior-identical **from
evidence local to the same function, or from an exhaustive enumeration of the
assignments to the name involved**.

**Freeze-lock** it when any of:
- **(a)** the fix changes a frozen public name, signature or manifest member;
- **(b)** the only fix is a `ClassVar` annotation — ground rule 5;
- **(c)** the fix requires the multi-line-import / aligned-block reflow the owner
  dropped on 2026-08-03;
- **(d)** equivalence cannot be established locally and rests on a whole-program
  reachability argument about a runtime type;
- **(e)** the fix changes the value, the type, or the exception raised on an edge
  or error path — including log text and traceback content, which §2 names as
  observable.

## 4. The behavior-affecting codes, and how each is discharged

Line numbers here, as in §5, are at the merge commit.

"Mechanical" describes the diff, not the risk. §2 permits exactly three PRs to
change observable behavior and this is not one of them.

- **`E721` (3)** — the fix is **`is` / `is not`, never `isinstance`**.
  `isinstance` accepts subclasses, and all three sites are in a package built on a
  subclass hierarchy: `pdsfile.py:747` is `elif type(self) == PdsFile` inside
  `__repr__`, where `isinstance` would change the repr of every `Pds3File` and
  `Pds4File`; `pdsviewable.py:572` is `type(pdsfiles) != list`, where `isinstance`
  would stop wrapping a list subclass; and `pdscache.py:322` is
  `type(port) == str` in `MemcachedCache.__init__`, where `isinstance` would send
  a `str` subclass down the `'127.0.0.1:%d'` branch — the one site neither the
  suite nor the probe can execute, because the constructor needs `pylibmc`.
  `type(x) is C` differs from `type(x) == C`
  only if the metaclass of `type(x)` overrides `__eq__`; every class here has
  metaclass `type`.
- **`F841` (9)** — removing a binding is safe only if the right-hand side has no
  effect. Two sites' right-hand sides **do**: `_preload.py:201` and
  `pdsfile.py:1116` are cache subscripts. Both keep the subscript as an expression
  statement bound to `_`; only the real binding goes.
- **`B904` (4)** — **all four freeze-locked**. Both available fixes change
  observable state: `from err` sets `__cause__` and `__suppress_context__` (the
  traceback changes from "During handling of the above exception…" to "…was the
  direct cause of…"), and `from None` suppresses the original traceback outright.
  There is no behavior-preserving variant. Resolves the B904 half of **deferred
  entry 37**.
- **`RUF005` (8)** — `a + [b]` → `[*a, b]` differs when `a` is not a list. Seven
  sites have a locally provable list/tuple operand; `_properties.py:609` does not
  and is freeze-locked (§5).
- **`SIM102` (3) / `SIM103` (3) / `SIM114` (2)** — SIM102 and SIM114 collapse
  branches whose short-circuit order is unchanged (`if A: if B: S` ≡ `if A and
  B: S` when the outer body is exactly the inner `if`; `if A: X elif B: X` ≡ `if A
  or B: X`). SIM103's *bare* rewrite would change the returned object from the
  `True`/`False` singleton to the condition's raw value, so all three sites are
  fixed as `return bool(<condition>)` — which is also the form ruff's own message
  names. `bool(x)` invokes exactly the `__bool__`/`__len__` that `if x:` invoked
  and returns exactly the singleton the branch returned, so the condition's own
  type never enters the argument and no reachability claim is needed.
- **`UP031` (14)** — 13 sites take arguments that are provably `str`, or an
  explicit tuple literal matching the placeholder count, or `len(...)`; for all of
  those `'%s' % x` and `f'{x}'` agree, because `format(x, '')` is `str(x)`. The
  14th, `pdscache.py:325`'s `'127.0.0.1:%d' % port`, does not: `%d` truncates a
  float, `{port:d}` raises on one, and `{port}` prints it. Freeze-locked.
- **`UP024` (20)** — genuinely safe, and worth saying why rather than lumping in:
  `IOError` and `EnvironmentError` are not merely spelled differently in Python 3,
  they **are** `OSError` (`IOError is OSError`), so even the class name printed in
  a traceback is `OSError` today. All 20 fixed.
- **`N806` (2)** — both are locals inside one classmethod
  (`pdsfile.py:1416`, `:1445`); neither is part of any signature.
- **`UP004` (4)** — includes `class PdsFile(…, object)`. The Phase-5 addendum
  `plans/2026-07-27-addendum-phase5-mixin-base-order.md` already rules that trailing
  `object` is not a mixin, is not required, gives an identical MRO, and is left for
  PR-23; it also names the one line to drop with it,
  `test_the_class_statement_stays_in_pdsfile_pdsfile`'s
  `assert PdsFile.__bases__[-1] is object`, "which stops being meaningful once
  `object` is gone". Verification is a measurement, not an assumption:
  `PdsFile.__mro__` is dumped before and after and the two lists compared, and the
  mixin harness (`tests/api/test_mixin_collisions.py`, which discovers mixins by
  filtering `object` out of `__bases__`) is re-run.

## 5. The permanent set — 40 violations, enumerated

Line numbers are at the merge commit.

**`RUF005` (8) is a permanent, owner-chosen style exclusion**, added on
2026-08-03 after the rest of this section was written. `x + [y]` is the spelling
this project uses and `[*x, y]` is not wanted anywhere, so all seven sites this
PR had converted went back to their `rewrite` spelling and the eighth stayed put.
The `_properties.py:609` row below keeps its separate (d) argument, which is
independent of and unaffected by the style ruling. §11 records the revert.

| File | Code | n | Sites | Rule | Reason |
|---|---|---|---|---|---|
| `_index_rows.py` | `RUF005` | 1 | :162 | style | owner-chosen exclusion |
| `_opus.py` | `RUF005` | 1 | :246 | style | owner-chosen exclusion. `:268` is the same spelling on a subscript operand, which ruff does not flag |
| `_preload.py` | `RUF005` | 1 | :325 | style | owner-chosen exclusion |
| `_sorting.py` | `RUF005` | 4 | :188, :196, :200, :205 | style | owner-chosen exclusion |
| `__init__.py` | `F403` | 3 | :10, :12, :13 | (a) | the three star imports are what bind `Pds3File`, `Pds4File` and the rule modules onto the package. Removing them shrinks the frozen surface; "correcting" `from pdsfile import *` to `from .pdsfile import *` grows it. **Deferred entry 31** records both readings and asks for an owner decision; it stays open |
| `_derived_paths.py` | `A002` | 3 | :263, :280, :296 | (a) | the frozen `dir=` parameter of `log_path_for_*`, called by keyword from the tools. **Deferred entry 45**: PR-18 moved these three methods, so `A002`'s home is this file, not `pdsfile.py` as the plan's text says |
| `_properties.py` | `RUF005` | 1 | :609 | style + (d) | `self._info[:4] + (shape,)`. `_info_filled` is a **list** on two construction paths (`pdsfile.py:634`, `:690`); on those, `list + tuple` raises `TypeError` today while `(*…, shape)` would silently succeed. It is unreachable with a list only by a whole-program argument about `len(self._info[4]) > 2` |
| `_shelves.py` | `B904` | 1 | :172 | (e) | see §4 |
| `pdscache.py` | `UP031` | 1 | :325 | (e) | see §4 |
| `pdscache.py` | `RUF015` | 1 | :623 | (e) | `list(mydict.keys())[0]` raises `IndexError` on an empty dict; `next(iter(...))` raises `StopIteration`. `mydict` is the last loop iteration's dict and is not proven non-empty. The site is inside `MemcachedCache`, which ground rule 9 protects and which no test in this repo can exercise |
| `pdsfile.py` | `I001` | 1 | :84 | (c) | ruff's isort rejects this repo's aligned-under-the-paren continuation style **unconditionally** — a minimal already-sorted `from foo import (aaa,\n bbb,\n ccc)` still reports I001 — and rewrites it one name per line with a trailing comma. That is precisely the layout change the owner examined and dropped (addendum §3 names "the multi-line imports" first). The fix also re-attaches every comment in that block to the wrong import: it leaves "Nothing below references these ten" above a group that now contains `import os` and `import re`, which are referenced |
| `pdsfile.py` | `RUF012` | 16 | class body | (b) | mutable class attribute defaults (`VOLTYPES`, `SHELF_CACHE`, `SUBCLASSES`, …). The only ruff fix is a `ClassVar` annotation, forbidden by ground rule 5 |
| `pdsfile.py` | `B904` | 3 | :1418, :1824, :1874 | (e) | see §4 |
| `pdsviewable.py` | `B006` | 3 | :52, :114, :205 | (a) | frozen public signatures — `PdsViewable.to_dict(exclude=[])`, `PdsViewSet.__init__(viewables=[])`, `PdsViewSet.to_dict(exclude=['abspath', 'alt'])`. The plan is explicit: **do NOT "fix" B006, it is a manifest break** |

Everything else — **114 violations** — is fixed.

## 6. Ratchet arithmetic

`per-file-ignores` may only shrink. Before (14 entries, 78 code slots) → after
(11 entries, 14 code slots). **No file's "after" set contains a code its
`origin/rewrite` set did not**, which is the property that makes this a shrink;
the `RUF005` entries recreated on 2026-08-03 are all restorations of codes those
files carried at `96e5960`, not additions.

| File | Before | After |
|---|---|---|
| `__init__.py` | `F403, F841, I001` | `F403` |
| `_associations.py` | `UP024` | **entry removed** |
| `_derived_paths.py` | `A002` | `A002` |
| `_index_rows.py` | `RUF005, UP024` | `RUF005` |
| `_local_fs.py` | `B007, B905, E701, SIM103, SIM118, UP024` | **entry removed** |
| `_opus.py` | `RUF005, UP024` | `RUF005` |
| `_path_utils.py` | `E701, F841` | **entry removed** |
| `_preload.py` | `E501, E701, F841, RUF005, UP015, UP031` | `RUF005` |
| `_properties.py` | `E701, F841, RUF005, SIM114, UP024, UP031` | `RUF005` |
| `_shelves.py` | `B904, B905, F841, RUF059, SIM103, UP024, UP031` | `B904` |
| `_sorting.py` | `E701, RUF005` | `RUF005` |
| `pdscache.py` | `A001, E701, E721, F401, F541, F841, I001, RUF015, RUF059, SIM102, UP004, UP031` | `RUF015, UP031` |
| `pdsfile.py` | `B904, C405, E501, E701, E713, E721, F841, I001, N806, RUF012, SIM102, SIM114, SIM118, UP004, UP024, UP031` | `B904, I001, RUF012` |
| `pdsviewable.py` | `B006, B007, B020, C405, E701, E721, F401, I001, RUF015, RUF059, UP004, UP024` | `B006` |

Every "after" code appears in §5 with a site list and a reason. No code is added
to any entry and no entry gains a file. `pdsviewable.py`'s `RUF059` is **already
dead** in the committed ratchet — the derived set has no `RUF059` in that file —
so it leaves as a stale-entry removal rather than as a fix.

**No inline `noqa` is added anywhere**; the ratchet's own comment forbids it. This
is checked mechanically (`git diff | grep noqa`).

`scripts/gen_ruff_ratchet.py` is **not** used to produce the new block:
**deferred entry 33** records that it emits an empty block against a tree whose
committed ignores already suppress everything, which is exactly this tree. The
block is derived from the no-ignores run described in §2 and then verified by
running the project config and requiring `ruff check src/pdsfile tests scripts`
to be clean and `--statistics` over the fifteen files to report only the §5 set.

## 7. Two `F401`s that are fixed rather than frozen

`pdsfile.pdscache.sys` and `pdsfile.pdsviewable.pdslogger` are **frozen manifest
members**, so deleting either import is a manifest break. They are not
freeze-locked all the same: `pdsfile.py` already establishes this repo's explicit
re-export form for exactly this situation — `import bisect as bisect`, with a
comment saying "the redundant `as` alias is the explicit re-export form" — and
ruff honours it (`pdsfile.py` binds ten unused stdlib modules that way and reports
no `F401`). So both become `import sys as sys` / `import pdslogger as pdslogger`
with the same comment. The name stays bound, the manifest is unchanged, and the
violation is genuinely gone rather than suppressed.

## 8. Deferred entries this PR owns

- **45** — resolved: `A002`'s permanent home is `_derived_paths.py`, and §5
  enumerates it there. The entry is marked resolved.
- **60** — mixed in-class banner rule-line widths. Measured over indented
  `#`-only lines across all fifteen files: the in-class convention is **80
  columns** (32 lines), with exactly **six** outliers forming three banners —
  `_preload.py:160/162` and `pdsfile.py:519/521` at 90 columns, `_preload.py:460/462`
  at 84. Those six lines are normalized to 80. This is whitespace **inside
  comments**: no token changes, and it is verified by tokenizing every touched
  file before and after and comparing the non-comment token streams. The two
  8-space `#` lines at `pdsfile.py:989`/`:994` are blank lines inside a comment
  paragraph, not banner rules, and are left alone.
- **64** — the six commented-out `MemcachedCache.get_multi` lines in `pdscache.py`
  (`:699`, `:1009–1013`). Entry 64 says removing them **needs an owner decision
  that has not been given**. They are **not** touched, the entry stays open, and
  the PR description says so.
- **31** and **37** get resolution notes (see §5); both stay open.

New deferred observations this PR must record, because it touches the lines that
carry them and may not repair them:

- `pdsfile.py:1110–1115` — the comment says "return from cache if available" and
  there is no `return`; the cache lookup's result is discarded.
- `_properties.py:1339–1340` — `version_ranks` assigns a **local**
  `version_ranks_filled = []` where every sibling branch assigns
  `self._version_ranks_filled`, so the property returns `None` for a file that
  does not exist.
- `_local_fs.py:410–416` — with the unused `value` renamed, the `values` list and
  the `zip` it feeds are visibly dead weight.

## 9. Commit sequence

Renames and fixes are never mixed (§2 PR discipline); there are no renames here at
all, so the split is by kind of change:

1. `docs: correct PR-23's stale heading in the modernization plan` — the plan's
   PR-23 heading loses "and format". Plan-only.
2. `docs: add the PR-23 sub-plan` — this file.
3. `style: ruff-clean the extracted core modules` — the ten `_*.py` modules.
4. `style: ruff-clean pdsfile.py` — including `UP004` on the class statement and
   the one assertion in `tests/api/test_mixin_collisions.py` that depends on it.
5. `style: ruff-clean pdscache.py and pdsviewable.py`.
6. `style: normalize in-class banner rule widths` — deferred entry 60, comment
   whitespace only, no token change.
7. `build: shrink the core ruff ratchet to the freeze-locked set` — `pyproject.toml`
   plus `.cursor/rules/pdsfile_overrides.mdc` deviation (4), which must record the
   same set.
8. `docs: record PR-23 validation` — `critiques/phase5-validation.md`,
   `critiques/pr-23/`, `critiques/deferred-observations.md`.

## 10. Verification

| Gate | How |
|---|---|
| §6.2 full-data, `--mode ns` | serial, under coverage, as `scripts/automated_tests/pdsfile_main_test.sh` runs it, `-rA --junitxml`. Baseline re-measured in a worktree at `96e5960`: **858 passed / 34 skipped**. Gate: **identical id-by-id set** |
| §6.2 full-data, `--mode s` | same; baseline **555 passed / 3 skipped**. Gate: identical set |
| non-vacuity | `coverage.CoverageData.measured_files()` must list all fifteen in-scope modules |
| no-holdings | `scripts/run-all-checks.sh` with no holdings env vars; baseline **92 passed / 800 skipped** |
| API freeze | `scripts/dump_public_api.py` at base and at HEAD; **an empty diff, byte for byte**. Any diff is a hard stop |
| MRO | `PdsFile.__mro__` dumped at base and at HEAD; lists compared |
| token equality | for the banner commit, `tokenize` every touched file at base and at HEAD, drop `COMMENT`/`NL`, compare |
| ruff | `ruff check src/pdsfile tests scripts` clean with the project config; and the no-ignores run over the fifteen files reports exactly the §5 set and nothing else |
| clean install | `scripts/clean_install_check.sh` |
| consumer smoke | `critiques/baselines/consumer-smoke-baseline.md`; gate is **same outcome** — rms-opus 4/4, rms-viewmaster 5 ok / the same 3 failures. Fewer failures is as much a flag as more |
| §6.6 | fresh, no-context opus-class reviewer per round, hard cap 4. Because almost every round touches `src/pdsfile/`, the full-data record is **regenerated each round**; no stale record is carried forward |

## 11. As executed

Appended after execution, as §4 of this file requires. Everything above is the
plan as written before any code changed, **with two exceptions, both recorded
here rather than silently**:

1. **§1's file table and §2's count were edited in place** by commit `f2f2416`,
   because the original said `ls src/pdsfile/*.py` gives sixteen files. It gives
   fifteen in a fresh checkout: `_version.py` is generated by setuptools-scm and
   matched by `.gitignore`'s `**/_version.py`, so it exists only after an install.
   That is a factual correction to a premise, not a change of plan, but the
   sub-plan's own method says to append rather than edit, so it is noted.
2. **§3–§6 were updated after adversarial rounds 1 and 3** — round 1 found that
   two `SIM103` violations classified freeze-locked were in fact fixable (see
   below), and round 3 found §4 still citing base-tree line numbers while §5 had
   been converted to head numbers.

### What changed against the plan

| Planned | Executed | Why |
|---|---|---|
| Commit 3 covers "the ten `_*.py` modules" | it also covers `__init__.py` | `__init__.py`'s two fixes are one blank line and one `as err`; a separate commit for them would be noise |
| Freeze-locked set of **35** | **33** | round 1's Major: `SIM103` ×2 in `_local_fs.py` is fixable as `return bool(...)` |
| Ratchet 8 entries / 11 slots | **7 entries / 10 slots** | same |
| 8 commits | the 8 planned, plus `f2f2416` (the `_version.py` correction), plus the review loop's | rounds 1–3 each produced a source-fix commit and a record commit; round 4, being scoped and record-only, produced one. `git log origin/rewrite..HEAD` is the authority; the per-round detail is in `critiques/pr-23/round-<k>.md` |

### The classification rule, in hindsight

Rule **(d)** — "equivalence cannot be established locally" — is the one that
needed care, and it is the one round 1 caught. The failure mode was to evaluate
*ruff's suggested rewrite* rather than *the space of behavior-preserving
rewrites*: `SIM103`'s bare collapse really does change the returned object, but
`return bool(<condition>)` does not, and this PR was already using that form
elsewhere. **(d) is a statement about the fix, not about the violation**, so the
question to ask at each site is "is there *any* spelling that is provably
identical", not "is ruff's spelling identical". The three remaining (d)/(e)
freeze-locks were re-checked against that standard and hold: `_properties.py`'s
`RUF005` has no spelling that both keeps the `TypeError` on a list `_info` and
satisfies the rule; `pdscache.py`'s `%d` has none that reproduces float
truncation; `pdscache.py`'s `list(d)[0]` has none that keeps `IndexError`.

### Verification actually performed

Everything §10 lists, plus one item the plan did not anticipate: because the
full-data run reaches only **81 of the 143** executable lines this PR changed, a
**differential probe** of the unreached fixes was written and run under both
trees — **55** labelled values, three independent base/head run pairs, all
identical. Round 2 grew it from 39 by adding the `MemcachedCache` methods, using
the `__new__`-plus-stub-client technique `tests/core/test_pdscache_set_multi.py`
already uses; measured under `coverage`, the suite reaches 81 of the 143 changed
executable lines, the probe 36 (24 the suite misses), the union 105.
It is what actually discharges `E721`, `RUF015`, `B020`, `A001` and the two
`F401` re-exports, none of which the suite executes. Details in
`critiques/phase5-validation.md` §2. The probe is scratch evidence, not a
committed test: PR-23's gate is an identical test-id set, and a new test id is
movement.

### Deferred entries this PR produced

**67**–**70** from the executor's own measurements, **71**–**72** from round 1,
**73** from the differential probe, **74**–**75** from round 2 and **76**–**77**
from round 3 — eleven in all. Entries **45** and **60** are resolved; **33** is
resolved as documented rather than fixed; **37** is half resolved; **31** and
**64** are untouched and still owed an owner decision.

## 12. Revision — four owner corrections, 2026-08-03

Given after the review loop had closed clean. Applied as four commits on top of
`f59ec05`, each correction its own commit, then re-validated end to end.

### 1. `RUF005`'s rewrite is not wanted; the exclusion is permanent

All seven `[*a, b]` conversions reverted to their `rewrite` spelling — verified
**byte-identical**, not merely equivalent, by extracting each line from
`git show origin/rewrite:<file>` and requiring an exact match in the working
tree: `_index_rows.py:162`, `_opus.py:246`, `_sorting.py:188/196/200/205`, and
`_preload.py:325`'s three-line tuple concatenation. `git diff origin/rewrite`
now shows none of those lines.

`RUF005` returns to `per-file-ignores` for the four modules whose entry this PR
had deleted because `RUF005` was their last code, and to
`pdsfile_overrides.mdc` deviation (4) as a permanent owner-chosen style
exclusion — not a residue awaiting cleanup, and explicitly binding on PR-24 too.
`pdsfile.py`, `pdscache.py` and `pdsviewable.py` were checked and need no
`RUF005` entry: they carried none at `96e5960` and have none now.

Re-derived with the template select set and no `per-file-ignores`, `ruff 0.15.22`
over the fifteen in-scope modules (the generated `_version.py` excluded, per
entry 71): **154 at `96e5960` → 40 here**, so **114 fixed**, not the 121 §5 and
§6 claimed before the revert. §5 and §6 above are updated to the new figures.

Deferred observation **75** is closed by the revert; its `:271` was re-measured
and is `:268`.

### 2. Logging takes lazy `%` arguments

`UP031` had turned four logging calls into f-strings. All four now pass a
`%`-style format string with the value as an argument. Exception messages keep
their f-strings, which is what the owner asked for.

Emitted text proven rather than assumed, because `pdslogger` reinterprets a lone
argument as a `filepath` when the message has no substitution pattern. All four
messages keep a `%s`/`%d`, which `_message_uses_args`'s `%[^(]` matches. A probe
ran each site in three spellings — `rewrite`'s eager `%`, this PR's f-string, and
the lazy form — over 18 value cases, through a real `PdsLogger` with a capturing
handler and again through the `EasyLogger`/no-handler path. Handler path: 18 of
18 identical. `EasyLogger` path: 14 of 18 identical, and the four that differ are
values containing a literal `%`, where **both** eager spellings raise
`TypeError: not enough arguments for format string` and emit nothing while the
lazy form logs correctly. No text any existing spelling emits changes.

An AST sweep of the rest of the package found **130** pre-existing eager logging
call sites (34 core, 96 subpackage). They are outside a `ruff check` PR's
warrant and are recorded as deferred observation **79**, not converted.

### 3. Comments describe the code, not the port's scaffolding

Eleven sites, each decided individually. Three named `critiques/` or a deferred
observation; eight named the frozen public surface, the API freeze or
`api_manifest.json`. Where the comment existed to stop a reader deleting an
import that looks unused — the `sys` and `pdslogger` re-exports and the three
`pdsfile.py` import blocks — that information is kept and restated in terms of
the code ("re-exported for callers that reach it as `pdsfile.pdscache.sys`").

Prose only, and measured: tokenizing all fifteen core modules before and after
with `COMMENT`/`NL` dropped gives 12 of 15 byte-identical token streams; the
other three differ in exactly one `STRING` token each, at identical token counts
— the three sites that are docstrings rather than `#` comments. The manifest
records names and kinds, never docstrings, and the manifest diff is empty.

### 4. `pyproject.toml`'s plan commentary is allowed, but only for now

The ratchet header and the per-entry notes stay. The obligation to delete them
before the rewrite merges to `main` is now an explicit **PR-37 deliverable** in
the plan's Phase 8 section, rather than living only in a PR description.

### Re-validation after the corrections

| Gate | Result |
|---|---|
| §6.2 `--mode ns` vs `96e5960` | 892 ids both sides, **858 passed / 34 skipped** both sides, 0 ids added, 0 removed, 0 outcomes changed |
| §6.2 `--mode s` vs `96e5960` | 558 ids both sides, **555 passed / 3 skipped** both sides, 0/0/0 |
| non-vacuity | `coverage.CoverageData.measured_files()` lists all fifteen in-scope modules |
| no holdings | `scripts/run-all-checks.sh`, all holdings vars unset: **92 passed / 800 skipped**, every check green |
| API freeze | fresh dumps at `96e5960` and at HEAD, 733,876 bytes each, **`diff` empty** |
| `PdsFile.__mro__` | identical at base and HEAD for `PdsFile`, `Pds3File`, `Pds4File` |
| `ruff check src/pdsfile tests scripts` | clean |
| consumer smoke | A 4/4 resolve, 0 failures; B 5 ok / 3 failures — the same three. Same outcome as baseline |
