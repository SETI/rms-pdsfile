# Sub-plan: combined PR-03 + PR-04 (Phase 1 — template adoption)

Per owner instruction (2026-07-23): do PR-04 (adopt `repo_template` support
files) and PR-03 (consolidate tool config into `pyproject.toml`) as **one**
actual PR, building the new `pyproject.toml` from the template version first.
Template placeholders: **`REPONAME` → `rms-pdsfile`** (distribution),
**`MODULENAME` → `pdsfile`** (import module). Phase 1 is config-only — **no
source moves**; the package stays at `pdsfile/`, tests stay where they are.

Template source: `/seti/all_repos/rms-devenv/repo_template`.

## Confidentiality (hard rule)
No absolute holdings path may be hardcoded in any committed file, and no
authentication token, credential name, or runner-secret filename may be
introduced into new files. The existing self-hosted CI already loads its runner
secrets and uploads coverage on the self-hosted runner — that is pre-existing
infra; leave it as-is (behavior-preserving), do not rewrite or extend it beyond
the two edits below. Holdings roots are always resolved from the
`PDS3_HOLDINGS_DIR` / `PDS4_HOLDINGS_DIR` env vars.

## Part A — PR-04 template files (do first)

1. **`.cursor/rules/`** — copy every template `*.mdc` **except `filecache.mdc`**
   (ground rule 6), with placeholder substitution. Keep `logging.mdc`.
2. **`.cursor/rules/pdsfile_overrides.mdc`** (NEW, `alwaysApply: true`) — the 10
   repo-specific deviations from plan PR-04 that take precedence over the
   template rules. Verbatim content list in plan lines 489-517.
3. **`.cursor/skills/`**, **`.cursor/settings.json`**, **`.vscode/settings.json`**
   — copy with substitution.
4. **`.github/ISSUE_TEMPLATE/*`**, **`.github/pull_request_template.md`** — copy
   with substitution.
5. **`CONTRIBUTING.md`** — copy from template with substitution (overwrite
   existing).
6. **`.readthedocs.yaml`** — copy (RTD build may be red until Phase 7; OK on
   rewrite).
7. **`.gitignore`** — adopt the template's (comprehensive standard Python) and
   keep repo-specific lines (`**/_version.py`). No tracked build/coverage
   artifacts exist to remove (verified: `htmlcov/`, `coverage.xml`, `*.egg-info/`
   already untracked + ignored).
8. **`publish_to_pypi.yml` / `publish_to_test_pypi.yml`** — adopt template
   versions (adds `twine check`, checkout@v6/setup-python@v6, `permissions`),
   substitute, but **keep our existing `on:` triggers** (plan: triggers
   unchanged).
9. **`.github/workflows/run-tests.yml`** — do NOT replace with template; only add
   a `pull_request: branches: [rewrite]` trigger so every rewrite PR gets the
   self-hosted full-data run.
10. **`scripts/automated_tests/pdsfile_main_test.sh`** — add `tests/api/` to the
    `--mode ns` pytest invocation so the API-freeze test runs in the self-hosted
    gate from now (§6.1). One-line edit; leave everything else.
11. **`scripts/run-all-checks.sh`** — adopt template, substitute, and **stage the
    enables** so only the three PR-04 gates are on:
    - `ENABLE_RUFF_CHECK=true`, `ENABLE_PYROMA=true`, new `ENABLE_API_FREEZE=true`.
    - `ENABLE_PYTEST=false` (hermetic pytest lands PR-11/14), `ENABLE_SPHINX=false`
      (PR-31), `ENABLE_PYMARKDOWN=false` (PR-31/34), `ENABLE_RUFF_FORMAT=false`
      (PR-23/24), `ENABLE_MYPY/BANDIT/VULTURE=false` (never).
    - ruff targets: `src tests` → `pdsfile holdings_maintenance utility scripts conftest.py`.
    - Add an **api-freeze check** to `run_code_checks` (new `RUN_API_FREEZE` /
      `ENABLE_API_FREEZE`, `--api-freeze` flag, wired into
      `_code_checks_any_scheduled`, `-c`, and the default-all block): runs
      `python -m pytest tests/api/test_api_freeze.py --confcutdir=tests -p no:cacheprovider -q`.
      `--confcutdir=tests` bypasses the pre-PR-09 root `conftest.py` (which still
      requires holdings to import), so the check is hermetic. PR-09 makes the
      conftest skip-aware; the `--confcutdir` can be revisited then.
12. **`scripts/read-docs.sh`** — copy with substitution.

## Part B — PR-03 pyproject.toml (build from template, adapt)

Start from `repo_template/pyproject.toml`, substitute, then adapt for
config-only Phase 1 (current non-src layout) and the locked ground rules:

- `[project].name = "rms-pdsfile"`; real one-line `description` (pyroma docks
  short ones); real `keywords`; `authors` + `maintainers` (R. French); classifiers
  unchanged; add `Documentation` URL.
- `dependencies` = the current runtime set (`numpy, pillow, pyparsing, pytest,
  range_ex, rms-pdslogger>=3.1.1, rms-pdstable, rms-translator, rms-textkernel`).
  **`pytest` stays a runtime dep until PR-08** (comment saying so). `tabulate`
  is NOT a runtime dep.
- **No `[tool.mypy]`, `[tool.bandit]`, `[tool.vulture]`** (ground rule 5 / never).
- `[tool.pytest.ini_options]`: `addopts = ["--strict-markers", "--strict-config"]`
  (NO `-n`/xdist, NO `--cov`); register marker `full_holdings`. **No** `pythonpath`
  or `testpaths` yet (src move is Phase 2; PR-07 adds `pythonpath=["src"]`). **No**
  `filterwarnings=error`.
- `[tool.setuptools] packages = ["pdsfile", "holdings_maintenance"]` (current
  explicit list — NOT `where=["src"]`; PR-05/06 switch that). **No**
  `package-data py.typed` (no `py.typed` until PR-35).
- `[tool.setuptools_scm] write_to = "pdsfile/_version.py"` (not `src/...` yet),
  `local_scheme = "no-local-version"`.
- `[project.optional-dependencies]`:
  - `dev = [coverage>=7.0, pymarkdownlnt>=0.9.35, pyroma>=4.2, pytest>=7.0,
    pytest-cov>=4.0, pytest-xdist>=3.8.0, ruff>=0.8, tabulate, "rms-pdsfile[docs]"]`.
    **DEVIATION from literal MODULENAME→pdsfile:** the self-referential extra
    uses the **distribution** name `rms-pdsfile[docs]`, not `pdsfile[docs]` — a
    literal `pdsfile` here is a different/nonexistent distribution and would
    break `pip install .[dev]`. (Flag to owner.)
  - `docs = [myst-parser, sphinx>=7, sphinxcontrib-mermaid, sphinx-rtd-theme]`.
- `[project.scripts]` — unchanged (still `holdings_maintenance.pds3...:main`;
  PR-06 repoints).
- `[tool.ruff]`: `target-version="py310"`, `line-length=100`,
  `[tool.ruff.format] quote-style="single"` (configured but NOT gated — PR-23/24),
  `[tool.ruff.lint] select=[E,F,W,I,UP,B,SIM,C4,A,N,PT,RUF]`,
  `extend-ignore=["PT011","SIM105","SIM108"]`, and an **auto-generated
  `[tool.ruff.lint.per-file-ignores]` ratchet**: run
  `ruff check pdsfile holdings_maintenance utility scripts conftest.py
  --output-format json`, group by (file, code), emit one entry per file with its
  current codes so `ruff check` passes. Ratchet may only shrink later.
- `[tool.coverage]`: port the existing `.coveragerc` (branch, `exclude_lines`,
  html title) + template `source = ["pdsfile"]`, `omit = ["tests/*","_version.py"]`.
  **DEVIATION from template:** NO `fail_under` and NO `parallel=true` — the
  self-hosted `pdsfile_main_test.sh` runs `coverage report` / `coverage run -a`
  without `coverage combine`, and current coverage is <90 (untested tools, #82),
  so either would turn the existing self-hosted gate red. (Flag to owner;
  `fail_under` can return in Phase 8 once #82 raises coverage.)
- `[tool.pymarkdown]`: adopt template (md013/md033 disabled).
- **`requirements.txt` → `-e .`**. **Delete `.flake8`, `.coveragerc`, `setup.cfg`**.

## Gates (all must pass before the PR opens)
- `pip install -e .` succeeds; `import pdsfile` works (no holdings).
- `ruff check pdsfile holdings_maintenance utility scripts conftest.py` → clean
  (ratchet).
- `pyroma .` clears (score high enough that the gate passes).
- API-freeze test passes (hermetic, via `run-all-checks.sh --api-freeze`).
- `scripts/run-all-checks.sh` green with exactly its three PR-04 gates on.
- Config-only: no source touched, so the full-data pass/fail set is unchanged by
  construction. Confirm the suite still COLLECTS/RUNS (install + a small real-data
  collection) and that the API-freeze test runs under the self-hosted script path.
- §6.6 adversarial review to convergence.

## Deviations to flag to owner (summary)
1. dev extra self-ref uses `rms-pdsfile[docs]` (distribution name), not literal
   `pdsfile[docs]`.
2. coverage config omits template's `fail_under=90` and `parallel=true` to keep
   the existing self-hosted gate green.
