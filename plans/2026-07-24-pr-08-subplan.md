# Sub-plan: PR-08 — extract rule-module tests (Phase 3), split version

Owner decision (2026-07-24): **split** PR-08. This PR does the mechanical
extraction + retain-verbatim standardization + applicability README, fully
behavior-preserving and verified against the **limited** testing holdings. The
plan's **additive-coverage** step (adding missing `test_versions` /
`test_duplicated_products` / `test_associations` and generating their goldens
with `--update` against full holdings) is **deferred to a follow-up PR** — the
owner first wants to understand why some full-holdings goldens don't reproduce
on the local `/data/pdsdata` copy while passing on the limited copy (both are
expected to pass). Do **not** touch goldens in this PR.

## Ground truth (grep-confirmed 2026-07-24, HEAD of rewrite after #99)

16 rule modules have inline tests (13 pds3, 3 pds4); 18 modules carry
`import pytest`; `pdsfile_test_helper` is imported only by the two
`rules/pytest_support.py`.

`pdsfile_test_helper.py` public defs: `instantiate_target_pdsfile`,
`read_or_update_golden_copy`, `read_file`, `write_data_to_file`,
`opus_products_test`, `associated_abspaths_test`.

pds3 `pytest_support.py`: `TEST_RESULTS_DIR`, `translate_all`,
`unmatched_patterns`, `versions_test`, + re-exports of the 3 helper funcs;
imports `pds3file`, `os`, `re`, `translator`. pds4 `pytest_support.py`: only
`TEST_RESULTS_DIR` + re-exports (different golden dir).

Per-module extraction map (test-section = banner→EOF unless noted; `used` =
names the test bodies reference and the extracted file must import):

| module | test funcs (beyond core 3) | used names | special |
|---|---|---|---|
| COCIRS_xxxx | +test_associations_to_volumes, +_to_diagrams | pds3file, translate_all | banner clean |
| COISS_xxxx | (core 3) | pds3file, os | **underscore helpers `_PDS4_REPROJ_BUNDLE_MARKERS` + `_coiss_opus_products_golden_references_pds4_reproj` (~776-794) move WITH the tests**; rule module KEEPS `import os` (still used at line 1031) |
| CORSS_8xxx | +test_default_viewables, +test_associations | pds3file, os, translator, translate_all, unmatched_patterns | banner clean |
| COUVIS_0xxx | (core 3) | pds3file, os | banner clean |
| COUVIS_8xxx | +test_versions | pds3file, versions_test | banner clean |
| COVIMS_0xxx | (core 3) | pds3file | **rule module needs `import os` ADDED (production `os.path.basename` at :347 relied on star-import)** |
| COVIMS_8xxx | (core 3) | pds3file, os | banner clean |
| EBROCC_xxxx | (core 3) | pds3file | banner clean |
| GO_0xxx | +test_duplicated_products | pds3file | banner clean |
| HSTxx_xxxx | (core 3) | pds3file | banner clean |
| NHxxxx_xxxx | (core 3) | pds3file | banner clean |
| VGISS_xxxx | (core 3) | pds3file | banner clean |
| VG_28xx | (core 3) | pds3file | banner clean |
| cassini_iss_fring_mosaics_rsfrench2025 (pds4) | (core 3) | pds4file | **inline `import pytest` + `from .pytest_support import (...)` sit just above tests (~387); cut from there; convert the import** |
| cassini_uvis_solarocc_beckerjarmak2023 (pds4) | (core 3) | pds4file | same inline-import shape |
| uranus_occs_earthbased (pds4) | (core 3) | pds4file | same inline-import shape |
| cassini_iss (pds4) | **none** | — | strip dead `import pytest` + `from .pytest_support import *`; no test file (only a commented `instantiate_target_pdsfile` ref) |
| cassini_vims (pds4) | **none** | — | same: strip dead imports, no test file |

Core-3 signatures are already uniform (`test_opus_products`,
`test_associated_abspaths`, `test_opus_id_to_primary_logical_path`); do NOT
harmonize. Extras retained verbatim, appended after the core 3.

## Execution recipe

1. **`tests/support/`**: `git mv src/pdsfile/pdsfile_test_helper.py
   tests/support/pdsfile_test_helper.py`; add empty `tests/support/__init__.py`.
2. **`tests/rules/support.py`** (merge both `pytest_support.py`): define
   `PDS3_TEST_RESULTS_DIR` and `PDS4_TEST_RESULTS_DIR` from the file's own
   location (`os.path.join(os.path.dirname(__file__), '..', 'golden', 'full',
   'pds{3,4}')` + `/`); `translate_all`, `unmatched_patterns`, `versions_test`;
   re-export `associated_abspaths_test`, `instantiate_target_pdsfile`,
   `opus_products_test` from `tests.support.pdsfile_test_helper`. `__all__` so
   the (non-star) explicit imports are clean. Delete the two
   `src/pdsfile/pds{3,4}file/rules/pytest_support.py`.
3. **Extract** each module's test section to `tests/rules/pds3/test_<ds>.py` /
   `tests/rules/pds4/test_<ds>.py` with a reconstructed explicit import header
   (`import pytest`; `import pdsfile.pds{3,4}file as pds{3,4}file`; `import os`
   /`import translator` where used; `from tests.rules.support import
   PDS{3,4}_TEST_RESULTS_DIR as TEST_RESULTS_DIR, <helpers used>`). NO star
   imports in test files (avoids F403/F405 — not in the migrated code set).
   COISS: include the underscore helpers. pds4: convert the inline
   `from .pytest_support import (...)` to `from tests.rules.support import (...)`.
4. **Strip** from each rule module: the test section, top-of-file `import
   pytest`, and `from .pytest_support import *` (pds4: the inline block went with
   the cut). Add `import os` to COVIMS_0xxx. Strip dead imports from
   cassini_iss/cassini_vims.
5. **conftest**: `git mv conftest.py tests/conftest.py`; repoint its helper
   imports to `from tests.pds{3,4}file.helper import ...` (unchanged form — works
   with repo root on path).
6. **pyproject**: `[tool.pytest.ini_options] pythonpath = [".", "src"]` (add repo
   root so the `tests.*` absolute imports resolve regardless of conftest
   location); **drop `pytest` from `[project].dependencies`** (it stays in the
   dev extra). Migrate/regenerate the ruff ratchet for the new test files +
   `support.py` + `tests/conftest.py`; rule-module entries shrink.
7. **CI** (`scripts/automated_tests/pdsfile_main_test.sh`): the rule-module
   `*.py` globs no longer hold tests — replace with `tests/rules/pds3/`
   /`tests/rules/pds4/`; keep `tests/api tests/pds3file tests/pds4file`.
8. **Clean-install gate** (NEW, permanent): fresh venv, `pip install .` (no
   extras), `import pdsfile` + import every PR-02 manifest module; add to
   `run-all-checks.sh` (+ `ENABLE_CLEAN_INSTALL`) and CI. Only gate that catches
   a runtime-dep leak.
9. **`tests/rules/README.md`**: applicability table — for each dataset, which
   extra tests exist and, for the ones it lacks, whether the underlying rule
   table (`VERSIONS`/`OPUS_PRODUCTS`/`ASSOCIATIONS`) is null (→ N/A) or non-null
   (→ **deferred additive coverage**, pending the golden-reproducibility
   investigation).
10. **Verify**: `pip install -e ".[dev]"`; full `--mode ns` and `--mode s`
    against the limited holdings (expect the same 679/34 ns + 555/3 s split,
    re-tallied after the rules `*.py` drop out of the invocation — the test
    COUNT is unchanged, only their file locations); `run-all-checks.sh` green
    incl. clean-install; api-freeze passes (manifest forgives the rule modules'
    lost `test_*`/`pytest`/`pytest_support`/`os` names — category #2).

## API-manifest note
Category #2 forgiveness (pre-approved): rule modules lose `test_*`/fixtures and
the `pytest_support`/`pytest`/`os` test-only names; `pdsfile_test_helper` leaves
the package (already manifest-excluded). Expect the freeze to pass; if it flags
the removals, they match the category predicate.

## Gotchas locked in
- `pythonpath = [".", "src"]` is what makes `tests.rules.support`,
  `tests.support.pdsfile_test_helper`, `tests.pds{3,4}file.helper` resolve as
  clean absolute imports after the conftest leaves the repo root.
- COISS keeps `import os` (production use at :1031); COVIMS_0xxx gains it.
- cassini_iss / cassini_vims: strip dead imports, no test file.
- No goldens touched; additive coverage deferred.
