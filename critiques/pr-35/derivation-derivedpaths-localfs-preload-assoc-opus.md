# Notes: frag-rest (27 members: _associations 4, _derived_paths 11, _local_fs 4, _opus 3, _preload 5)

## Per-member evidence

- associated_abspaths | (self, category: str, must_exist: bool = True) -> list[str] | _associations.py:169-344; every return is a list of str: new_abspaths built from checksum_path_and_lskip()[0]/archive_path_and_lskip()[0] (str, lines 248, 270), abspaths from glob_glob (list[str]), pattern literals, pdsf.abspath, and dedup comprehensions (lines 258, 280, 343-344)
- associated_logical_paths | (self, category: str, must_exist: bool = True) -> list[str] | _associations.py:138-139 returns cls.logicals_for_abspaths(abspaths); _sorting.py:715-738 returns [logical_path_from_abspath(p, cls) for p in abspaths], str per path
- associated_parallel | (self, category: str | None = None, rank: int | str | None = None) -> PdsFile | None | _associations.py:346-558; returns None (line 465), cls.from_logical_path(category) (line 469, itself PdsFile | None), cached lookups `cls.from_abspath(abspath) if abspath else None` (lines 491, 517), and _cache_and_return(...) which returns a PdsFile or None (lines 438-454). rank accepts None, an integer rank, or 'latest'/'previous'/'next' (lines 494-513)
- associated_pdsfiles | (self, category: str, must_exist: bool = True) -> list[PdsFile] | _associations.py:165-167 returns cls.pdsfiles_for_abspaths(abspaths); _sorting.py:694-712 builds [cls.from_abspath(p) ...] — from_abspath can hand back cache entries and child()-selected subclass instances, so list[PdsFile], not list[Self]
- archive_logpath | (self, task: str) -> str | _derived_paths.py:321-352; returns this.log_path_for_bundle('_targz', ...), a str
- archive_path_and_lskip | (self) -> tuple[str, int] | _derived_paths.py:263-267; abspath = ''.join([...]) (str), lskip = len(...)+len(...)+len(...) (int); raises ValueError on checksum/archive files and missing bundle name (lines 246-261)
- archive_path_if_exact | (self) -> str | _derived_paths.py:269-298; returns '' (lines 283, 286, 291, 298) or path_if_exact (str, line 296)
- checksum_path_and_lskip | (self) -> tuple[str, int] | _derived_paths.py:76-139; returns (abspath, lskip), str join and int arithmetic on all three branches (lines 116-134); raises ValueError otherwise
- checksum_path_if_exact | (self) -> str | _derived_paths.py:141-182; returns '' or the path (lines 168, 180, 182)
- dirpath_and_prefix_for_archive | (self) -> tuple[str, str] | _derived_paths.py:316-319; two f-strings
- dirpath_and_prefix_for_checksum | (self) -> tuple[str, str] | _derived_paths.py:202-211; two f-strings on both branches
- log_path_for_bundle | (self, suffix: str = '', task: str = '', dir: str = '', place: str = 'default') -> str | _derived_paths.py:504-532; delegates to _log_path_for, which returns ''.join(parts) (line 502)
- log_path_for_bundleset | (self, suffix: str = '', task: str = '', dir: str = '', place: str = 'default') -> str | _derived_paths.py:534-559; same delegation to _log_path_for
- log_path_for_index | (self, task: str = '', dir: str = 'index', place: str = 'default') -> str | _derived_paths.py:561-594; same delegation; raises ValueError for non-index files (line 590-591)
- set_log_root | classmethod (cls, root: str | None = None) -> None | _derived_paths.py:358-381; writes cls.LOG_ROOT_ (None or root.rstrip('/') + '/'), no return
- glob_glob | classmethod (cls, abspath: str, force_case_sensitive: bool = False) -> list[str] | _local_fs.py:475-614; returns [abspath], [], _clean_glob(...) (filesystem glob result, list of str paths), a list comprehension of str replacements (line 559), or [p.rstrip('/') for p in abspaths] (line 614)
- os_listdir | classmethod (cls, abspath: str) -> list[str] | _local_fs.py:294-473; every return is a list of str basenames: shelf keys (line 370), comprehensions appending '_md5.txt'/'.tar.gz' (lines 383-425), os.listdir-derived lists (lines 440-441, 472-473), aareadmes + filtered (line 469)
- os_path_exists | classmethod (cls, abspath: str, force_case_sensitive: bool = False) -> bool | _local_fs.py:111-220; returns os.path.exists(...) (bool), `key in shelf` (bool), True/False literals, and `basename in childnames` (bool); the lru_cache wrapper does not change the signature
- os_path_isdir | classmethod (cls, abspath: str) -> bool | _local_fs.py:222-292; returns bool comparisons (checksum == '', line 262), True literals, `not abspath.lower().endswith('.txt')` (line 290), os.path.isdir (line 292)
- from_filespec | classmethod (cls, filespec: str, fix_case: bool = False) -> PdsFile | _opus.py:78-113; returns cls.from_logical_path(BUNDLE_DIR_NAME + '/' + bundleset + '/' + filespec, fix_case). from_logical_path (pdsfile.py:1687-1767) returns None only for an empty path, unreachable here because bundleset is verified truthy (line 109-110); its other returns are cache entries and child()-built objects, provably PdsFile but not provably Self (child selects the right subclass for the path, which need not be a subclass of the class called on). Raises ValueError when the translator recognizes no bundle set
- from_opus_id | classmethod (cls, opus_id: str) -> Any | _opus.py:115-192; line 156-157 returns pdsfile_class.OPUS_ID_TO_PRIMARY_LOGICAL_PATH(opus_id) verbatim when that attribute is a plain function — an untyped rule-module call, so Any; line 186 returns fake_product_dict[fake_opus_key][0][0] after an untyped opus_prioritizer hook rebinding; lines 174, 192 return PdsFile. Union with Any collapses to Any
- opus_products | (self) -> dict[Any, list[list[PdsFile]]] | _opus.py:194-410; values are lists of sublists of objects built by from_abspath and pdsfiles_for_abspaths (lines 334, 350-352, 374-380), re-sorted in place (lines 387-408); keys flow from opus_type_for_abspath (second elements of untyped OPUS_PRODUCTS translator patterns, line 266) or pdsf.opus_type (untyped lazy property, line 360), so the key type is not provable — docstring says five-element tuple or ''
- cache_category_merged_dirs | classmethod (cls) -> None | _preload.py:489-506; only cls.CACHE.set calls, no return
- cache_lifetime | classmethod (cls, arg: Any) -> int | _preload.py:803-825; returns cache_lifetime_for_class(arg, cls), whose every branch returns an int module constant (lines 133-148); arg is any cacheable object (str, PdsFile, bookkeeping dict/list)
- get_permanent_values | classmethod (cls, holdings_list: str | list[str] | tuple[str, ...], port: int) -> None | _preload.py:273-343; no return statement; holdings_list is forwarded to cls.preload (line 336) "in whatever form preload() accepts"; port forwarded unchecked
- load_volume_info | classmethod (cls, holdings: str) -> None | _preload.py:345-487; holdings is joined with '_volinfo' (line 399); no return statement
- preload | classmethod (cls, holdings_list: str | list[str] | tuple[str, ...], port: int = 0, clear: bool = False, force_reload: bool = False, icon_url: str | None = None, icon_color: str = 'blue') -> None | _preload.py:508-801; lines 565-568 accept a single path or a list/tuple and map _clean_abspath over str paths; port is compared to 0 and stored on cls.MEMCACHE_PORT (int, lines 571, 579); icon_url None builds per-holdings defaults (lines 780-782), otherwise used as str; the early `return` (line 653) and fall-through both yield None. Viewmaster passes port= per the class docstring (lines 253-261)

## Instance attributes

- `permanent` (bool) — _preload.py:707, `pdsdir.permanent = True` inside preload's interior `_preload_dir()`, set on every directory the preload visits. This is the only PUBLIC instance attribute these five files create on a PdsFile instance. It is assigned through the local name `pdsdir`, not literally `self.`, but it lands on PdsFile instances; nothing in the package reads it back.
- Not new attributes, for completeness: _derived_paths.py:347-350 assigns `this.checksums_ = ''`, `this.archives_ = ''`, `this.category_ = this.bundletype_` on a discarded copy inside archive_logpath — these public-named attributes are core PdsFile state initialized in `__init__`, not introduced here. _associations.py:486 assigns `self._associated_parallels_filled` (private, excluded). _preload.py also mutates `pdsdir._childnames_filled` in place (private, and a mutation not an assignment). set_log_root/_pinned_log_timetag write the CLASS attributes `LOG_ROOT_` and `_LOG_TIMETAG`, not instance attributes.

## Discrepancies

- _opus.py:144 vs _opus.py:156-157 — from_opus_id's docstring says "Returns: PdsFile: the primary data file", but when a rule subclass implements OPUS_ID_TO_PRIMARY_LOGICAL_PATH as a plain function the method returns that untyped call's result verbatim ("what that branch returns is whatever the rule module chose to return", per its own docstring at line 132-134), and line 186 returns an element pulled from the untyped opus_prioritizer hook's rebound dictionary. Declared `Any`, not `PdsFile`.
- _opus.py:251-253 vs _opus.py:360 — opus_products' docstring says each key is "a five-element tuple, or the empty string", but keys come from an untyped translator table's pattern tuples or from the untyped `opus_type` lazy property, so the tuple shape is not provable from this code. Declared `dict[Any, list[list[PdsFile]]]` (broader than the docstring's `dict[tuple[str, int, str, str, bool] | str, ...]`).
- _opus.py:100-101 (from_filespec docstring "Returns: PdsFile") vs pdsfile.py:1722-1724 — the delegate from_logical_path returns None for an empty path. Unreachable here (bundleset is checked truthy at _opus.py:109-110 before the join), so `PdsFile` without `| None` is declared; recorded because the docstring's claim rests on that reachability argument, not on the delegate's own contract. Also NOT declared `Self`: from_logical_path returns cache entries and child()-selected subclass instances, which are PdsFile but not provably instances of the class called on.
- members-rest.json signatures for the classmethods omit the `cls` parameter (they are runtime bound-method signatures); the stub lines add `cls` back, which is the standard stub form. Parameter names and defaults otherwise match the JSON exactly, including `dir` (shadowing the builtin) in the three log_path_for_* methods and `dir='index'` in log_path_for_index.
- _local_fs.py:112 — os_path_exists is wrapped in `functools.lru_cache(maxsize=200)` under the classmethod decorator; the stub presents it as a plain classmethod, which matches its call signature. Noted so the flattener does not "restore" a decorator.

## Imports needed

- `from typing import Any` (used by from_opus_id, opus_products, cache_lifetime)
- `PdsFile` self-reference in annotations (forward reference within the flattened class stub; legal in .pyi)
- No `Self`, no `collections.abc` names, no `_Translator`/`_PdsLogger`/`_PdsTable` aliases needed by this fragment
